{% extends 'layout.html.twig' %}

{% block logout_link %}{{ path('admin.logout') }}{% endblock %}
{% block logo_link %}{{ path('admin.index') }}{% endblock %}

{% block navigation %}
    {% set menu = knp_menu_get('menu_admin') %}
    {{ knp_menu_render(menu, {template: 'menu/navbar.html.twig'}) }}
{% endblock %}
