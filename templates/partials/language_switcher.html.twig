{% for locale in enabled_locales %}
    <a href="?_locale={{ locale }}" class="{% if locale == app.locale %}font-bold text-zebrelina-dark-blue{% endif %}">{{ locale }}</a> {% if not loop.last %}/{% endif %}
{% endfor %}
